[package]
name = "imgterm"
version = "0.1.0"
edition = "2021"

[dependencies]
image = { version = "0.25", default-features = false, features = ["jpeg", "png", "gif", "bmp", "tiff", "webp"] }
terminal_size = "0.3"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
